/**
 * Medusa seed betiği (Faz 1 placeholder).
 * `medusa exec ./src/scripts/seed.ts` ile çalıştırılır.
 */

import type { ExecArgs } from '@medusajs/framework';

export default async function seed(_args: ExecArgs): Promise<void> {
  // eslint-disable-next-line no-console
  console.log('[commerce-backend] seed çalıştırıldı — Faz 4'te ürün verisi yüklenecek.');
}
