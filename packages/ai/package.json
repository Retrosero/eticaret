{
  "name": "@eticart/ai",
  "version": "0.1.0",
  "description": "AI/LLM abstraction (OpenAI, Anthropic) + EtiCart AI features",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@eticart/config": "workspace:*",
    "@eticart/shared-types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vitest": "^1.6.0"
  }
}
