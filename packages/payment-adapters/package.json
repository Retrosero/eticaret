{
  "name": "@eticart/payment-adapters",
  "version": "0.6.0",
  "private": true,
  "description": "Ödeme adaptörleri (iyzico, PayTR, Param, havale, kapıda ödeme).",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./iyzico": {
      "types": "./dist/iyzico/index.d.ts",
      "default": "./dist/iyzico/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "lint": "eslint src --max-warnings=0",
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "tsc -p tsconfig.build.json",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@eticart/config": "workspace:*"
  },
  "devDependencies": {
    "@eticart/eslint-config": "workspace:*",
    "@eticart/shared-types": "workspace:*",
    "@eticart/tsconfig": "workspace:*",
    "eslint": "^9.15.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.5"
  }
}
